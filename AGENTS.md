# AGENTS.md

## 代码注释规范

写代码注释时只描述「当前功能」或「怎么用」，禁止任何过程性/变更叙述。

- 禁止：`// 新增了 xxx 校验`、`// 本次修复了焦点问题`、`// 删除了旧的分支逻辑`、`// 之前用 A，现在改成 B`，也禁止「避免刚点按钮就提示…」这类叙述某个 bug 被修掉的注释。
- 允许：`/** 将焦点移到验证码输入框 */`、`// 表单校验异步返回，稍后再清一次，避免提示被写回`（描述当前实现行为，而非本次改动）。
- 变更历史写进 commit message，不写进代码注释。

适用于本仓库所有语言与文件。
